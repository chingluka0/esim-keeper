# Screenshots

App screenshots referenced by the README files:

- `home.jpg` — card list (light)
- `home-dark.jpg` — card list (dark)
- `add-card.jpg` — add card screen
- `billing.jpg` — number billing dialog (with import preset)

Guidelines:

- Use lowercase English file names (e.g. `home.jpg`, `card-list.jpg`, `billing-preset.jpg`).
- Avoid non-ASCII (Chinese) file names so GitHub renders them reliably.
- Do not include private numbers, real verification codes, emails, or ID documents.
